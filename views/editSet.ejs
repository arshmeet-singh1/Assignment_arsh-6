<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= set.name %> - LEGO Set</title>
    <link rel="stylesheet" href="/css/main.css"> <!-- Your Tailwind CSS or custom styles -->
    
</head>

<body>
    <!-- Include Navbar (with an empty page value) -->
    <%- include('partials/navbar', { page: '' }) %> <!-- Empty page value since this is not a navbar page -->

    <!-- Main Content -->
    <div class="container mx-auto mt-8">
<!-- Hero Section -->
<div style="background-color: #e0f2fe; min-height: 50vh; display: flex; justify-content: center; align-items: center;">
    <div style="text-align: center;">
        <div style="max-width: 400px;">
            <h1 style="font-size: 2.5rem; font-weight: bold;">Edit Set: <%= set.name %></h1>
            <p style="padding: 1.5rem 0;">Use the below form to update the <%= set.name %> set.</p>
        </div>
    </div>
</div>

<!-- Edit Set Form -->
<form action="/lego/editSet" method="POST" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-top: 1.5rem; padding: 1.5rem; background-color: #ffffff; box-shadow: 0 4px 8px rgba(0,0,0,0.1); border-radius: 0.5rem;">
    
    <!-- Set Name -->
    <div style="margin-bottom: 1.5rem;">
        <label for="name" style="display: block; color: #4a4a4a; font-weight: 500;">Set Name:</label>
        <input type="text" name="name" value="<%= set.name %>" required style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; transition: border-color 0.2s; focus: outline: none; focus: border-color: #3b82f6; focus: background-color: #e9d5ff;">
    </div>

    <!-- Year -->
    <div style="margin-bottom: 1.5rem;">
        <label for="year" style="display: block; color: #4a4a4a; font-weight: 500;">Year:</label>
        <input type="number" name="year" value="<%= set.year %>" required style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; transition: border-color 0.2s; focus: outline: none; focus: border-color: #3b82f6; focus: background-color: #e9d5ff;">
    </div>

    <!-- Number of Parts -->
    <div style="margin-bottom: 1.5rem;">
        <label for="num_parts" style="display: block; color: #4a4a4a; font-weight: 500;">Number of Parts:</label>
        <input type="number" name="num_parts" value="<%= set.num_parts %>" required style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; transition: border-color 0.2s; focus: outline: none; focus: border-color: #3b82f6; focus: background-color: #e9d5ff;">
    </div>

    <!-- Image URL -->
    <div style="margin-bottom: 1.5rem;">
        <label for="img_url" style="display: block; color: #4a4a4a; font-weight: 500;">Image URL:</label>
        <input type="url" name="img_url" value="<%= set.img_url %>" required style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; transition: border-color 0.2s; focus: outline: none; focus: border-color: #3b82f6; focus: background-color: #e9d5ff;">
    </div>

    <!-- Theme Selection -->
    <div style="margin-bottom: 1.5rem;">
        <label for="theme_id" style="display: block; color: #4a4a4a; font-weight: 500;">Theme:</label>
        <select name="theme_id" required style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; transition: border-color 0.2s; focus: outline: none; focus: border-color: #3b82f6; focus: background-color: #e9d5ff;">
            <% themes.forEach(theme => { %>
                <option value="<%= theme.id %>" <%= (set.theme_id == theme.id) ? "selected" : "" %>><%= theme.name %></option>
            <% }); %>
        </select>
    </div>

    <!-- Set Number (Read-only) -->
    <div style="margin-bottom: 1.5rem;">
        <label for="set_num" style="display: block; color: #4a4a4a; font-weight: 500;">Set Number:</label>
        <input type="text" name="set_num" value="<%= set.set_num %>" readonly style="width: 100%; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.75rem 1rem; background-color: #f3e8ff; cursor: not-allowed;">
    </div>

    <!-- Button Row -->
    <div style="grid-column: span 3; display: flex; justify-content: space-between; margin-bottom: 1.5rem;">
        <!-- Submit Button -->
        <button type="submit" style="background-color: #3b82f6; color: white; padding: 0.5rem 1.5rem; border: none; border-radius: 0.375rem; cursor: pointer; transition: background-color 0.2s;">
            Update Set
        </button>
        <!-- Delete Button -->
        
  
</form>
<form action="/lego/deleteSet/<%= set.set_num %>" method="GET" style="margin-left: 1rem;">
    <button type="submit" style="background-color: #f43f5e; color: white; padding: 0.5rem 1.5rem; border: none; border-radius: 0.375rem; cursor: pointer; transition: background-color 0.2s;">
        Delete Set
    </button>
</form>
</div>
    </div>

</body>

</html>
